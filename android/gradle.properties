org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Production backend base URL (override at build time; never commit credentials).
# Leave empty to fall back to the placeholder in app/build.gradle.kts.
apiBaseUrl=
debugApiBaseUrl=http://10.0.2.2:5000
