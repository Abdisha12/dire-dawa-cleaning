export default function Page() {
  return (
    <div className="rounded-lg bg-[var(--surface)] p-8 text-center shadow-[var(--shadow)]">
      <h2 className="text-lg font-semibold">Safer Zones — Locations/Safer Zones</h2>
      <p className="mt-1 text-sm text-[var(--text-muted)]">108 zones, leader assignment — migrated in Phase 4. Foundation shell / tokens / API client ready.</p>
    </div>
  );
}
