export default function Page() {
  return (
    <div className="rounded-lg bg-[var(--surface)] p-8 text-center shadow-[var(--shadow)]">
      <h2 className="text-lg font-semibold">Users — Administration/Users</h2>
      <p className="mt-1 text-sm text-[var(--text-muted)]">4 roles + fayda, RBAC — migrated in Phase 4. Foundation shell / tokens / API client ready.</p>
    </div>
  );
}
